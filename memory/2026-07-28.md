# 2026-07-28

## Decisions
- Recommended replacing Qwen2.5-7B with Qwen3-4B Q4_K_M for local LLM on Hermes VM
- Recommended adding Qwen3-0.6B as speculative decoding draft model for 2-3x additional speedup
- Classified 9 local LLM crons by sensitivity: 3-4 truly need local (finance, tax, health PII), 5 can route to gemini-local
- Identified llama.cpp was likely not compiled with AVX-512/AMX despite CPU support

## Actions Taken
- Created `scripts/llm-upgrade.sh`: Complete VM upgrade script (pip install, llama.cpp rebuild, model download, server restart, verification)
- Created `scripts/push_llm_upgrade.py`: Notion bridge pusher with minimal relay message (~1,200 chars context for Allie)
- Created diagnostic artifact analyzing all issues and 4 model alternatives
- Created deployment walkthrough with expected performance improvements

## Outcomes
- Deployment package ready: Jon runs `python3 scripts/push_llm_upgrade.py` from Mac → Allie receives and executes
- Expected improvement: 0.75 tok/s → 8-15 tok/s (10-20x), 200-token briefing from 4.5 min → ~20 sec
- Two "failed" crons (weekly-fitness-overview, HM1) just need `pip install python-telegram-bot` — not an LLM issue

## Open Questions
- Exact format of `~/.hermes/scripts/llm-watchdog.py` on VM — Allie needs to update it to use new startup script
- Which health crons specifically were on llama-local beyond the 6 listed in the diagnostic
- Whether Qwen3 GGUF filenames from Qwen official repo match exactly (fallback URLs provided)

## Recommended Skills
- `financial-automation` — affected cron routing
- `health-automation` — affected cron routing
- `home-maintenance` — cron moving to gemini-local
- `project-board` — should track this upgrade as a project
