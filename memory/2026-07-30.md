# 2026-07-30

## Decisions
- Adopted "Antigravity builds, Allie executes" as the core architecture principle
- Chose FastAPI on port 8787 for bridge server (separate from Hermes Gateway on 9119)
- Cloudflare Tunnel (trycloudflare.com) over SSH/Tailscale — port 22 firewalled, work Mac can't install Tailscale
- Keeping Notion relay as permanent fallback per Jon's preference
- Corrected cron auditor model routing: body metrics/cost review stay on gemini-local, training intel stays on gemini-local
- Top 5 crons chosen by monthly token spend (frequency × cost per run), not just per-run cost

## Actions Taken
- Generated ed25519 SSH keypair at `~/.ssh/id_ed25519`
- Built FastAPI bridge server (`bridge/server/main.py`, 7 endpoints, auth middleware, path traversal protection)
- Built bridge CLI client (`scripts/bridge.py`, push/pull/send/read/status/health)
- Deployed bridge via Allie (Telegram instructions → Notion push → Cloudflare Tunnel)
- Created `configs/model_routing.yaml` — maps all crons to optimal model tiers
- Created `configs/skill_manifest.yaml` — status of all 22 skills
- Created 5 cron runbooks in `runbooks/` (30-44 lines each vs 300+ line SKILL.md)
- Pushed all 5 runbooks to Allie via bridge — first real pipeline test
- Upgraded `allie-skill-builder/SKILL.md` — HTTP bridge is now primary transfer channel
- Upgraded `bridge.py` push command — supports skills and runbooks with clean remote paths
- Allie confirmed all 5 crons detached from SKILL.md and attached to runbooks

## Outcomes
- **Bridge operational**: 6 endpoints verified, sub-second latency via Cloudflare Tunnel
- **Persistence**: supervisord manages bridge-server (pid 80199) and bridge-tunnel (pid 80200)
- **Token savings**: ~67K tokens/month reduction across top 5 crons (~70%)
- **Pipeline proven**: Build locally → push via bridge → Allie installs. Total cycle: ~10 minutes
- **Tunnel URL**: `https://cable-trades-isaac-afford.trycloudflare.com` (ephemeral)

## Cron Job IDs (from Allie)
- drive-health-reader: `528453d02306`
- hevy-daily-sync: `08da8095d023`
- CAL2-Calendar: `e3d478ec2100`
- weekly-training-intelligence: `9e122c602629`
- weekly-fitness-overview: `aab1cd5e047b`

## Open Questions
- Named Cloudflare Tunnel — needs Cloudflare account + domain. Low priority since ephemeral URL works.
- Remaining 13 crons — runbooks would save more tokens but diminishing returns (lower frequency)
- Port 8787 / port 22 firewall — Abacus SuperComputer doesn't expose custom ports. Cloudflare Tunnel is the workaround.

## Recommended Skills
- `health-automation` — runbook is live, monitor first cron execution to verify output matches template
- `calendar-automation` — CAL2 runbook live, verify Sunday execution
- `health-planner` — both weekly runbooks live, verify Sunday/Monday execution
- `financial-automation` — next candidate for runbook creation (Monthly Financial Update = 2K tokens/run)
- `allie-skill-builder` — upgraded, ready for next skill build cycle
