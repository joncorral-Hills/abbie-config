# Work Log — 2026-05-28

## Decisions
- None

## Actions Taken
- Diagnosed Telegram approval button authorization failure on Allie (Hermes agent)
- Root cause: `TELEGRAM_ALLOWED_USERS=@JonCorr` in `~/.hermes/.env` — Hermes requires numeric Telegram user IDs, not @usernames
- Fix applied:
  - Added `TELEGRAM_ALLOWED_USERS=7605388765` (numeric ID)
  - Added `TELEGRAM_ALLOW_ALL_USERS=true` (belt + suspenders)
  - Set `approvals.mode = smart` via `hermes config set`
  - Restarted gateway (`hermes gateway restart`)
- Approval buttons now working — Approve Once / Always Approve / Cancel inline keyboard renders and responds to clicks

- Configured auxiliary model routing on Allie: compression + vision → `deepseek/deepseek-v4-flash` (paid, $0.14/M)
- Migrated 13 cron jobs from `:free` / `gemini-3-flash` / null → `deepseek/deepseek-v4-flash` (heartbeats, reminders, status checks)
- Migrated 2 financial crons from `gemini-3-flash` → `moonshotai/kimi-k2.6`
- 14 crons unchanged (already on correct tier)
- Two crons (`Daily Task Proposal`, `openrouter-credit-check`) had been failing with empty responses on Gemini 3 Flash — model switch should fix

## Outcomes
- ✅ Telegram approval buttons functional — closes open question from 2026-05-27
- ✅ `approvals.mode = smart` active — AI auto-approves low-risk commands, buttons for dangerous ones
- ✅ Model tiering fully applied: DeepSeek V4 Flash (paid) for lightweight tasks, Kimi K2.6 for processing, Sonnet 4.6 for synthesis
- ✅ Auxiliary compression/vision routed to DeepSeek V4 Flash — saves ~$2+/week vs using primary model

## Open Questions
- `.env` has duplicate entries (old `@JonCorr` line, double `TELEGRAM_ALLOW_ALL_USERS`) — cleanup recommended but non-blocking
- Monitor the two previously-failing crons to confirm DeepSeek resolves the empty response issue
